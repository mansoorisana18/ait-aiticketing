You are evaluating whether a newly submitted support ticket is a duplicate of one of the candidate existing tickets.

Current ticket:
- Ticket ID: {ticketId}
- Text Version: {textVersion}
- Title: {title}
- Description: {description}

Candidate existing tickets (JSON):
{candidatesJson}

Configured duplicate thresholds:
- CONFIRMED threshold: {confirmedThreshold}
- POTENTIAL threshold: {potentialThreshold}

Instructions:
1. Choose the single best candidate primary ticket if a duplicate relationship exists.
2. Return duplicateState as one of:
   - NONE
   - POTENTIAL
   - CONFIRMED
3. Apply these exact threshold rules:
   - If similarity is greater than or equal to {confirmedThreshold}, return CONFIRMED.
   - Else if similarity is greater than or equal to {potentialThreshold}, return POTENTIAL.
   - Else return NONE.
4. If duplicateState is NONE, primaryTicketId must be null.
5. If duplicateState is POTENTIAL or CONFIRMED, primaryTicketId must be the best matching existing ticket ID.
6. confidence must be between 0 and 1.
7. similarity must be between 0 and 1 and should reflect semantic closeness.
8. threshold must be the exact threshold value that corresponds to the returned duplicateState:
   - CONFIRMED -> {confirmedThreshold}
   - POTENTIAL -> {potentialThreshold}
   - NONE -> {potentialThreshold}
9. Give a short reason.
10. Do not return a duplicateState that violates the threshold rules above.

Return ONLY raw JSON.
Do NOT wrap the JSON in markdown code fences.
Do NOT include ```json, ```, or any explanation text.
Return exactly one JSON object in this shape:
{
  "duplicateState": "NONE|POTENTIAL|CONFIRMED",
  "primaryTicketId": null,
  "confidence": 0.0,
  "similarity": 0.0,
  "threshold": 0.0,
  "reason": "..."
}